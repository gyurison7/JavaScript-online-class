// 함수
function add (x, y) {
    let result = x + y;
    return result;
}

let add2 = function (x, y) {
    return x + y;
}

console.log(add(2, 3));

let functionResult = add(3, 4);
console.log(functionResult);